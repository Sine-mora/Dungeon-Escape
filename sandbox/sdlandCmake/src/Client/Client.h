#ifndef CLIENT_H
#define CLIENT_H

#include "enet/enet.h"


class Client {
/*	private:
		ENetHost* m_client;
		ENetAddress m_address;
		ENetEvent m_event;
		ENetPeer* m_peer;

	public:
		Client();
		bool Init();
		bool CreateClient();
		bool SetHost(ENetAddress address);
		bool ConnectPeer();
		bool ConnectServer();
		void ReceiveComms();
		bool Disconnect();
		bool Shutdown();
		*/
};

#endif // !CLIENT_H